# schema51 独立终审

日期：2026-08-09
结论：**PASS**

## 终审结论

独立安全终审已对最新本地候选树复审，未发现剩余的确定性 P0/P1。
schema51 的三条产品链、数据迁移、权限范围、并发/幂等、计费/退款、
文件读取和 session 撤销已达到进入受控不可变发布的代码门禁。

PASS 仅适用于当前本地候选树，不等于生产发布、数据迁移或业务冒烟已经
完成。

## 需求完成审计

- **巡店经理：PASS。** 从门店、现场照片、问题证据、整改责任/期限到复查和
  人工验收均已落地；模型无证据时不能伪造发现，也无权自动关单。
- **持续交互：PASS。** 直接派活保留每轮版本并能继续第 2/3/4 轮；巡店经理
  通过整改/复查专用流继续，不被降级为无证据文本聊天。
- **行业老板看板：PASS。** owner 仅看本企业明确行业，命名 Boss 可跨范围选择，
  且不将跨租户结果链接到当前租户详情。平台没有证据的营收/ROI 明确
  显示未接入。
- **431 名员工口径：PASS。** 11 名核心员工与 420 名行业员工的编号、
  岗位与私有能力/技能绑定均有动态契约覆盖。

## 终审发现与已闭环修复

| 风险 | 闭环结果 |
|---|---|
| 同租户不同行业可能通过任务中心看到巡店记录 | 任务列表和详情都二次校验 `allowed_modules` 与 `inspection_visit.industry_key`，回归通过 |
| 同租户不同行业可能直接读取巡店图片 | 受管文件中间件由照片反查巡店行业并调用服务端权限校验，回归通过 |
| 停用用户/租户后旧 Cookie 可能在重启时复活 | 停用和 epoch 撤销收入同一事务，会话解析联查用户/租户状态，回归通过 |
| 并发 session 撤销存在读改写丢更新 | 改为单语句原子 UPSERT 累加，12 线程并发回归通过 |
| schema51 只迁移启用租户会让重启后行业映射丢失 | 迁移扫描所有租户的明确旧映射，不以 enabled 过滤，回归通过 |
| 看板把“本期”和“全部未关闭积压”混为一个口径 | 拆成 period 和 `all_open_records` backlog，风险指标只用明确口径，回归通过 |
| 命名 Boss 的跨租户下钻结果包含无法安全打开的伪路由 | 跨租户结果的 `target_route` 固定为空，仅同租户才生成详情链接，回归通过 |
| 通用 `updated_at` 会把历史任务的元数据更新伪装成今日交付 | 任务/巡店新增稳定终态时钟，退款使用独立结算时钟，重试、收养、编辑和恢复不改写经营历史 |
| 巡店任务成本/退款已计入员工卡，但顶部任务数和趋势口径不一致 | 通过 `visit.task_id + tenant + industry` 精确合并一次，总览、风险、趋势和员工卡一致，跨行业不重算 |
| 同租户跨模块和命名 Boss 跨租户可能通过已知 `/files` 路径读业务正文 | 全部受管文件类型反查最窄 `required_module`，所有角色必须精确同 tenant，且行业/模块权限实时生效 |

## 证据门禁

- 最终全量回归 `1182/1182` 通过。
- Python compileall、JavaScript 语法检查、发布 shell `bash -n`、
  `pip check` 和 `git diff --check` 全部通过。
- 独立安全复审在上述修复后给出 PASS，无剩余确定性 P0/P1。

## 进入发布的强制条件

- 从候选树构建全新不可变 release，不在当前生产目录直接改码。
- 切换前执行备份、固定升级器和 schema51 迁移，保留可验证回执和回滚门禁。
- 切换后验证服务/定时器/健康、正式 HTTPS 登录、连续多轮、巡店照片
  证据链和老板看板范围。
- 只有上述生产证据齐全后，才能在 `11-release.md` 声明上线完成。

---

## schema52 独立终审状态

日期：2026-08-10

结论：**PASS，最终锁定树无剩余确定性 P0/P1**

### 已完成的独立攻击审计

- 最大规模 20,000 门店 + 40,000 经营行可完成 preview/commit；解析预算、PII 加密、
  tenant/industry/request/row 绑定、密钥变化、CAS 重试、公式/宏/外链/ZIP/XML 攻击
  和视觉模型数据边界未发现确定性 P0。
- 第一版留存审计发现两个 P1：未提交预览只在本租户请求时惰性清理，无法保证
  不活跃租户 24 小时后消失；已提交导入永久保留逐行 staging，重复 60,000 行导入
  会令数据库约按每批五十余 MB 增长。
- 因此 schema52 第一版没有取得发布 PASS，也没有进入生产切换。

### v2 修复审阅范围

- 启动 + 周期调度均调用有界、跨租户 sweep，租户请求仍有局部快速清理；partial
  index 仅覆盖 `staging_purged_at IS NULL` 的候选，避免全表保留扫描。
- committed staging 改为包含完整 action 和脱敏数据的规范化 zlib 审计归档，使用
  SHA-256 内容寻址和去重；写入后回读校验再事务删除 working rows。
- 导入摘要、错误统计、源文件元数据、目录版本、摘要哈希、操作者、时间和归档坐标
  长期保留；`store_branch`、`inspection_business_value`、`inspection_visit` 及整改、
  计费等权威记录不在清理集合中。
- 大批量提交继续走 batch CAS；归档损坏时详情读取 fail-closed，不以未认证副本
  掩盖篡改。

### 最终独立复验

- 24h 跨租户清理、partial index、secure-delete、备份/WAL、长读者 busy 重试：PASS。
- 满量三轮、内容寻址去重、无线性增长、并发普通写不超时：PASS。
- action/error 认证、归档篡改 fail-closed、归档失败全事务回滚：PASS。
- 权威门店/经营/巡店表零误删，tenant/actor/industry 隔离，51→52 并发迁移：PASS。

不可变双构建、生产切换和切换后正式域名验收均已完成；最终证据见
`11-release.md`。当前锁定候选与生产验收均无剩余确定性 P0/P1。

---

## schema53 独立终审

日期：2026-08-12
结论：**GO；最终锁定候选未发现确定性 P0/P1。GO 不等于部署授权。**

### 审阅范围与最终判断

- 复核 10 个非餐饮行业 V2 目录的 60 名决策员工、64 个研究来源和 180 项结构化
  指标；目录口径已从趋同的通用岗位转为按行业痛点、决策对象、必需输入、证据和
  人工审批边界定义的独立合同。
- active 员工是新派活唯一入口；legacy 员工只解释历史。任务、连续协作与会议使用
  `emp_idx` 加五项冻结身份字段解析，目录版本、spec 或快照任何一项不一致均停止，
  不按当前编号猜测历史身份。
- 服务端 canonical provenance manifest 绑定 tenant/spec/RI/content hash，用户材料明确
  标为 `user_submitted_unverified`。系统只检查归属、覆盖、引用结构和确定性冲突，
  不把关键词重合当作真实性证明。
- GO 必须满足严格章节、每 RI 一条独立且在 PDF 中可见的事实/值/日期/记录、固定人工
  审批句和固定禁止自动写操作句；任何缺项、未知/伪造 token、状态冲突、完成态高风险
  副作用或导出不可见事实均 fail-closed 为 HOLD。
- standalone 编辑已经纳入同一门禁；门禁后的正文是导出与知识沉淀的唯一来源。

### 独立复验结果

- provenance/门禁专项 `35/35` 通过。
- 独立异常格式一致性验证 `220/220` 通过；全部 `170` 个 Unicode `Cf` 字符逐点验证
  无开放项，并覆盖 U+034F、变体选择符、NFKC、零宽/转义、RI/U 大小写与长短、
  重复 token、同线多 pair、复合状态及 ERP/POS 域冲突。
- PDF 一致性验证确认：将事实、日期或记录放进 HTML、Markdown 图片、链接目的地或
  引用定义等不可见位置时不能取得 GO；合法 GO 的证据 ID 和同行事实均在 PDF 可见。
- 审批/禁止固定句、全文“已执行”副作用陈述、部分 HOLD 覆盖文案、V1 字节级不变、
  followup 合并/幂等/扣费前拒绝与跨 tenant/spec 篡改均通过。
- 主任务最终证据分组为：决策 `104`、身份 `93`、发布 `205`、安全 `178`、前端
  `100`、巡店 `132`。分组存在重复覆盖，不能相加声称为一套全量用例。
- Python 编译、JavaScript 语法和 whitespace/diff 检查通过。
- `task_center` 全套在系统 Python 下有 2 个 Playwright 缺失导致的环境错误，无业务
  断言失败；前端执行者已用真实 Chromium 完成对应验证并通过。终审没有把这 2 个
  环境错误记成套件全绿，也没有据此判定产品失败。

### 非阻断观察

- **P2：** standalone V2 重复编辑可能形成嵌套 gate envelope；安全状态继续由外层
  门禁控制，不构成越权，但后续应安全解包服务端 envelope 或只编辑原始输出。
- **P2：** 前端“已冻结证据”建议调整为“已保存（未核验）”，与服务端
  `user_submitted_unverified` 语义完全一致。

### 生产边界

生产当前仍为 **schema52**。schema53 截至本次终审时 **未构建、未获部署授权、未部署**。
本次 GO 只允许候选进入后续受控构建/发布决策，不得作为已经上线或可以直接上线的证明。

---

## schema54 独立审阅最新状态

日期：2026-08-13

结论：**本地锁定候选独立终审 PASS，`P0=0 / P1=0 / P2=0`；该 PASS 不等于发布
或部署授权。** schema53 的“360 名 legacy/active=131”口径已被 schema54 的在岗模型
取代，但上方 schema53 历史审阅结论原样保留。

### 审阅判断

- 现役人数按“人”计算：非餐饮原员工 360 名（10×36、原 `idx`）、餐饮 60 名、
  核心 11 名，共 431 名；行业 active 为 420。V1 同工号 360 份和 V2 `20xxx` 60 份
  是岗位定义的 historical versions，不是 420 名被降级或被淘汰的员工。
- 本轮交付不是提示词改名工程。每一 current 岗位必须独立具备专业档案、行业知识、
  数据对象、inputs、workflow、outputs、只读 tools、skills、capabilities、escalation、
  learning、3 个行业原生 KPI 和痛点强度/频率/经济价值/数据可得性四维优先级；任何
  一组通过替换名词即可互换的合同都应阻断。
- 结构审计已取得 360 岗 `errors=0`、1,080 个 KPI 的 key/name/formula 三字段各自
  全局唯一；运行时计数合同为 `420 / 420 / 480 / 840`。这些数字证明目录和版本视图，
  不允许把 historical versions 计入现役人数。
- 十行业 80 个痛点簇采用独立语义审阅，不只检查链接或 source ID。审阅者必须确认
  来源的权威性、辖区和适用对象能直接支撑对应痛点与岗位判断；定向问题修复后的
  80 簇最终复核无阻断项。

### 身份与配置安全判断

- schema54 将稳定人员槽位与可版本化岗位配置分离，并以 full `identity_ref`、
  `config_revision`、配置 SHA-256 和 CAS row version 约束变更。
- task、task_thread、meeting 必须精确冻结同一身份和配置版本；重试/followup 沿旧
  岗位版本执行，新根任务使用 current V3。未知/歧义迁移资料、哈希篡改、同 `idx`
  猜测、跨线程/会议串岗或缺档回退均应 fail-closed 且零扣费。
- 写目录的权威实现只有 A、hotel+fitness、auto+beauty+pet 三个生成器；旧 B 生成器
  已退役并必须在写入前退出，避免双权威覆盖已经审定的 360 份目录。

### 终审关闭证据

- 隔离干净副本使用 Python 3.12 和 `requirements.txt` 完整依赖，并以真实 Chromium
  执行前端路径；全仓 `unittest discover` 为 `1456/1456 OK`。
- schema54 高风险专项为 `219/219`，但专项包含在全仓回归中或与其重叠，不与 1,456
  相加。目录 auditor、三权威生成器、退役 B 不可写和 `420/420/480/840` 身份计数均
  保持通过。
- 独立审阅完成目录语义、迁移回滚、同 `idx` 串岗、配置 CAS/哈希、任务/线程/会议、
  真实浏览器和生成器权威性复验；最终分级为 `P0=0 / P1=0 / P2=0`。
- 该 PASS 只证明锁定本地候选满足当前 schema54 合同；不得把 `1456/1456`、
  `219/219` 或 `errors=0` 表述为生产已经发布。

### 生产边界

截至 2026-08-13，没有为 schema54 构建正式不可变 release，没有新的部署授权，
也没有部署、上传制品或切换服务。生产仍运行既有 schema52；本节不改写 schema51–53
已经记录的历史发布事实。

---

## schema54 生产切换独立终审

日期：2026-08-13

结论：**GO；生产切换后独立只读终审 `P0=0 / P1=0`。** 上一节记录的是 D-035
授权和正式切换前的历史状态；当前生产已经是
`20260813T134506Z-schema54-r7` / schema54。

- RID 唯一回执为 `root:root 0600`、单硬链接，`status=succeeded`、
  `phase=complete`、candidate schema 54，post-commit proof 对切换后备份、完整性和
  正式 HTTPS 的判断均为 `ok`。
- `current`、contentcrew cwd/exe 精确指向新 release/venv；contentcrew、Caddy
  `Result=success`、退出码 0、`NRestarts=0`，两个备份 timers enabled/active，
  8899 只监听 loopback。
- 在线数据库和切换后备份均为 `54/54`，quick/integrity 均为 `ok`；切换后备份
  `db-2026-08-13T135541Z.db` 的创建时间晚于 cutover commit。
- 本机、`paihuo.ai`、`www.paihuo.ai` 三个 `/healthz` 均为 HTTP 200；artifact、
  materialized、wheelhouse、bootstrap、control-plane 五条证据链再次验证通过。
- 生产人数与版本视图复验为：核心 11 + 行业 current 420 = 431；current V3 360、
  餐饮 60；historical 420（V1 360 + V2 60）；all identities 840。current 420 的
  idx/key 唯一且均为 active/current/can-assign，没有发生减员、降级或同工号串岗。
- 切换前后执行队列均为 0，仅保留 1 条非执行 `awaiting_review` 历史工单；独立终审
  未创建业务数据、未触发模型或计费。Caddy 在停服阶段一次优雅终止超时后由
  fail-closed 机制强制停止并正常恢复，完成后无持续告警，不构成遗留缺陷。
- 新 release 的观察窗口截至 `2026-08-14T13:55:50.122479+00:00`；该窗口是上线后
  运行观察，不影响本次切换已经成功的结论，也不能在届满前写成观察完成。
