# 决策日志

- 2026-08-20 | D-056：副账号体系升级（schema57）——职级、行业绑定与数字员工级白名单，
  全部后端强制。①users 新增 job_title（director/manager/staff）与 allowed_emp_idxs_json
  （NULL=行业内全部，数组=白名单）；②权限语义：老板(owner)全权（职级/行业板块/员工名单/
  密码/启停/删除/模型配置）；总监可给经理和员工、经理可给员工分配数字员工，且只能分配
  自己行业内、自己可用的员工（自己被限定名单时不得放开为"全部"、不能给自己/同级/上级调）；
  员工只用被分配的。③强制点：楼层列表过滤（白名单外不可见）、员工详情 403、单独派活 403、
  会议建会校验+自动选人过滤；内容部流水线是整体板块不做员工级白名单；tour 维持板块级语义。
  ④团队页改版：职级徽章/行业标签/分配摘要、创建带职级、职级编辑、按行业分组的数字员工
  分配器（不限定开关+全选/清空）；总监/经理登录有受限「团队分配」视图（只见自己+下级，
  企业信息只回名称）；导航对可分配成员显示「团队分配」入口 | 老板：「子账号太简单要优化，
  职级老板/总监/经理/员工、行业绑定、经理按职责分配行业内数字员工、老板全权含模型」|
  app/db.py(v57)、app/auth.py、app/main.py、static/app.js、tests/test_team_hierarchy.py
  （15 项权限矩阵）。
- 2026-08-20 | D-055：老板视角展示层大改（行业市场 + 一眼懂介绍 + 上手指引），全部不动内箱。
  ①「集团楼层」改名「行业市场」，11 个行业部门显示名统一成「汽车行业/便利店行业/健身瑜伽
  行业…」——映射只在 main.py 输出层（_display_dept_name），目录文件、身份契约、任务快照、
  提示词继续用原始部门名，零哈希漂移；②员工介绍重写成老板一眼懂三段式（TA干的活/啥时候
  找TA/怎么用）：登录视角借 duty 引号内核心问题一句话（「帮您把关「…」」），岗位名自动转
  成事（去官/员/师后缀），游客视角不透 duty 原文；旧 intro 的「孔市场专职承担」类生成残次
  前缀彻底不再展示；③开工四步、发布三件套每步补「为什么+怎么做」，新增常驻「📖 数字员工
  怎么用·四步用人法」教程卡（挑人→派活→收活→沉淀 + 会议室/组队/工具箱进阶），可不再提示、
  重看引导可恢复 | 老板：「四步不够详细、三件套要完善、加用人教程、集团楼层改行业市场、
  部门改行业叫法、介绍要一眼看懂、别动内箱」| app/main.py 展示层、static/app.js、
  tests/test_boss_display_layer.py（9 项断言含内箱冻结校验）。
- 2026-08-20 | D-054：schema56 切流事故复盘——r22 树字节码污染二次复发与合同化恢复。
  r23 切流在 release_runtime_validation 失败、回滚 preflight 同因失败（rollback_failed、
  服务停机约 10 分钟）。根因与 0816 事故完全同源：r22 切流后在 /srv/paihuo/current 直接
  跑门禁回放测试没带 PYTHONDONTWRITEBYTECODE=1，往已认证的 r22 树写入 app/、app/skills/、
  tests/ 三处 __pycache__。恢复按上次先例合同化执行：清除 payload 区污染 → release_digest
  重算与 r22 原始认证值逐字节一致 → 原子修复回执（manual_repair 审计块、
  status=in_progress/phase=rollback_validating）→ launcher 完成 recovered_rollback（服务回
  r22、DB 55、schema56 隔离库保留）→ control_plane.py finish 收尾遗留 active wrapper 事务
  → 失败 RID 不复用，新 RID r24 全链重走一次成功（succeeded/complete、DB 56、三点 200）。
  固化规则：任何时候在生产发布树上跑 Python（含测试回放）必须 `PYTHONDONTWRITEBYTECODE=1
  python -B`，跑完 find 校验零 __pycache__；失败过的 release id 一律不复用 | 服务短暂停机
  由本次会话操作引入，已如实记录 | /var/lib/paihuo-upgrade 回执与 manual_repair 审计块。
- 2026-08-20 | D-053：会议室新增「Agent 团队协作执行」（schema56）+ 服务器体感优化。
  ①组队执行：建会时勾选 `team_execute` 后，GO/NEED_INFO 的行动不再各自并行，而是按分工
  接力——每一棒任务的材料自动携带前面队友的已交付正文（每棒截 1600 字），全部成员收口后由
  队长（第一行动负责人）执行固定文本的整合任务，输出一份最终交付包；整合行动以
  `team_role=integrate` 追加进 actions_json，任务仍走 (meeting, action key) 唯一索引幂等，
  重启由 resume_pending 按 team_execute 路由回团队编排器续跑，任务终态回写会议交付段不变。
  编排器自身不抛异常：任务一旦启动，会议只能如实收口不能回滚退款；一棒未派出则退回
  awaiting_execution 可手动重试。并行模式与旧会议行为完全不变（默认关闭）。
  ②服务器"卡"排查结论：负载/内存/磁盘全闲、本机接口 <10ms，瓶颈是中国→美西链路上的
  往返次数；已放行 UDP 443（HTTP/3/QUIC，Caddy 本就广告 h3 但防火墙只开了 TCP）、
  给带 ?v= 内容哈希的 /static/* 加 Cache-Control: public,max-age=31536000,immutable
  （repo deploy/Caddyfile 与线上同步改，API 响应不受影响）| 老板："会议模式里面加上
  agent team 功能。还有就是优化下服务器，现在有点卡" | app/db.py(v56)、app/meeting.py、
  app/main.py、static/app.js、tests/test_meeting_agent_team.py、deploy/Caddyfile、ufw。
- 2026-08-17 | D-052：V4 全量切 claude-opus-4-8（老板"全范围"）+ 决策门禁人机契约校准
  （r17/r18）。实测证明 10/10 HOLD 封面不是模型能力问题（opus 同样被拦），是门禁机器摩擦：
  ①状态词带说明判非法；②审批边界/禁止动作要求逐字复现服务端常量；③任何 URL 即违规（与
  "保留网页来源"的系统要求自相矛盾）；④RI 复述判伪造；⑤记录锚点/时间窗词表过窄（"出杯量/
  近两周/条例"不识别）；⑥子标题截断章节正文；⑦提示词教的引用格式与门禁要求不一致。校准原则
  "机器常量归机器,分析职责归员工"：GO 严格文法/防伪造/防自动执行全部保留原判（攻击测试
  不变绿转红）。老板中转 sonnet-5 为无消费方的半成品配置（custom_models 零引用），未接线。
  验证：#44/#46 证据单零违规通过（模型自判 HOLD 因规则单要求近4个周一数据而只给2周——合法
  严谨）；#47 无证据分析单剩 1 项来源合规提示（原 5-6 项）。遗留：无证据单的"具名来源"合规
  率待模型迭代 | 老板："全范围"+"失败原因优化到每个员工" | app/departments.py、
  tests/test_decision_delivery_gate.py、tests/test_decision_provenance.py、
  RID r17=20260817T152031Z、r18=20260817T154039Z、commit 6a01808。
- 2026-08-17 | D-051：全员数据终检 + 企业记忆三层化（r16）。终检器（431 员工×7 维：技能/档案组/
  详情覆盖/决策合同/出厂档案/bundle 哈希/槽位）结果：429 全绿，仅巡店经理(10)与超级店长·活动
  策划(160) 历史无技能——已用同一接地标准补齐（各 7 张带来源技能卡，$0.41），现 431/431 完备。
  信息架构评估：人事档案(身份/配置/能力包)、资产库(媒体制品)、沉淀库(人工筛选经验)、员工产出
  (task 交付)、企业档案(租户事实)五者职责不同、生命周期不同、契约测试各自锁定，**不做物理合并**；
  改为统一"企业记忆"访问层：context_block = 企业档案 + 知识沉淀召回 + 新增「近期相关交付摘要」
  召回（_delivery_block：同租户已完成任务 summary，相关性≥阈值才注入，≤3 条/1200 字符），
  全部数字员工共用该入口（流水线/专家任务/单独派活/工具箱），公司做过的活自动成为下一单的
  参考口径 | 老板："数据再检查别再犯错；档案/资产/沉淀/产出/企业档案能合就合不能合就改进；
  沉淀要进每个数字员工" | app/skills/registry.py、tests/test_knowledge_retrieval.py、
  RID 20260817T071617Z-schema55-r16。
- 2026-08-17 | D-050：工具箱/老板看板/热点线索/会议室升级第一批（r15，依据三份审计）。
  工具箱：私域日历 8→3(撰稿人)、口播矩阵 8→4(文风师) 角色纠配；今日必发/私域日历/起号/竞品/
  裂变/看图文案六条提示词按「任务定位/输入/硬性结构/质量门禁/禁止项」重写；今日必发与竞品盯梢
  接入 company_block（选题/结论必须贴企业）、热点新增可选 channel 字段（质量门放行）；warmup
  company_block 改异步。看板：日界 UTC→UTC+8（_period+6 处 SQL '+8 hours'，测试同步）；前端补
  10 个状态中文、KPI 加"本期模型花费($/点/tokens)"、"今日先处理"改"先处理清单(标注口径)"、
  巡店"未闭环问题"标总账、趋势表注明固定近14天。会议室：提案 rubric 加质量门（对齐验收标准/
  反空话/风险具体）、排序失败推送系统消息不再静默、决策 prompt 注入完整排序+「任一 FAIL/致命
  风险未化解不得 GO」硬映射+落选说明；前端新增 mtStructured 结构化渲染（提案卡+三视角验证矩阵，
  API 字段早已返回但从未画出）、老板介入仅 status=done 显示。前端另修：起号平台/人设选择回显、
  热点一键开工带行业+角度、隔日热点过期提示、线索降级分析显式标注。线索检索段不挂员工包为
  隔离设计（与任务研究一致），不改 | 老板："工具箱/看板/会议室全部升级" | app/growth.py、
  app/meeting.py、app/bossdashboard.py、static/app.js、tests/test_boss_dashboard.py、
  RID 20260817T060404Z-schema55-r15；审计代理 bf429bea/95452afa/034521ff。
- 2026-08-17 | D-049：泄露熔断校准（r13+r14）+ 每单装载凭证。老板拍板：每单任务必须实际调用
  能力与技能，正常运用不得熔断。排查证实 431 名员工中 429 名带 32 字级技能触发线（共 3140 条），
  健身/宠物只是先踩雷。修复：providers.leak_fingerprint_source 把技能/能力块（6 处调用点：
  build_task_prompt、registry.build_prompt/solo_prompt、meeting、巡店、growth 工具箱）与 V4 目录
  手册/职责句（仅 build_task_prompt，老板自定义模板保持逐行原文保护）压成单行指纹——只保留
  64 字滑窗 + 私有标题/标签启发式，整段倒卖仍熔断，短语级正常运用放行；交付规则加
  「用自己的话执行，不逐字复述内部原文」；taskrunner 每单写入可见凭证步骤
  「已装载批准能力包 rN：启用能力 X 项 · 进修技能 Y 条」。r13 后能力/技能不再触发但 V4 手册
  仍触发 → r14 补齐手册/职责降级。实测：健身 1701 此前 3 连熔断，r14 后 ADVISE 真交付
  （覆盖率 29.1% 测算，$0.32）；12/12 行业全部有真实分析产出 | 老板："失败原因给我看+查全员+
  技能能力每单必须调用" | app/providers.py、departments.py、skills/registry.py、meeting.py、
  growth.py、main.py、taskrunner.py、tests/test_prompt_confidentiality.py、
  RID r13=20260817T045656Z / r14=20260817T050900Z。
- 2026-08-17 | D-048：泄露检测指纹源回退 JSON 形态（r12 热修）+ 12 行业实战验收。D-047 把能力包
  注入改为可读中文后，敏感指纹从"64 字滑窗"变成"32 字逐行"，员工在交付里正常运用岗位口径被误判
  泄露（宠物 1901 首跑熔断）。修复：提示词保留可读渲染省 token，assert_no_private_leak 的指纹源
  改回与旧实现同构的紧凑 JSON（taskrunner approved_sensitive / meeting profile+bundle leak_source），
  保护粒度与优化前完全一致；execution_bundle 测试加锁。实战验收（15 派单、$4.62、点数 0）：
  餐饮 112/内容 0 直接交付质量好；10 个 V4 决策岗分析质量好且守合同（酒店联网证伪任务书车展、
  美容 ESCALATE 判越界、零食缺数自判 HOLD），但决策机器门禁因固定文本逐字+RI 精确引用格式，
  10/10 盖 HOLD 封面（完整分析保留在「原始输出（人工复核）」段）；证据全提交复测（#37）模型给出
  GO 仍因引用格式差一步被降级 → 默认模型 deepseek-v4-flash 格式服从率不足。健身 1701 三次
  防泄露熔断（技能卡明细同词，零扣费）。三个待老板拍板项：V4 切 claude-sonnet-5 路由 /
  门禁逐字校验放宽为语义等价 / 技能卡明细泄露阈值 32→64 | 老板要求每行业实测一名员工出报告 |
  app/taskrunner.py、app/meeting.py、tests/test_schema55_execution_bundle.py、
  RID 20260817T034043Z-schema55-r12、canvas industry-live-test-report。
- 2026-08-17 | D-047：提示词结构优化（去重 + 缓存友好前缀）。审计发现：任务/会议 system 末尾
  整包注入 effective bundle 的压缩 JSON（实测 V4 任务 5.3~5.7k、会议再叠 12k 档案 JSON），其中
  技能明细、能力项、决策合同与 system 前文按同一批准配置渲染的文本块完全重复；且
  industry_block 按任务方向召回、插在 system 中段，把跨任务字节级稳定前缀拦腰截断，DeepSeek
  等供应商的自动前缀缓存永远无法命中（默认模型 deepseek-v4-flash）。改法：
  (1) `employees.approved_role_context_text`/`profile_context_text` 共享渲染器——版本指纹
  （revision+config_sha16+bundle_sha16）+ 未渲染部分全文（V4 档案可读渲染）+ 已渲染部分标题级
  引用（技能只留标题、目录一致的决策合同只引用；effective 与目录漂移时防御性全文注入）；
  (2) taskrunner/meeting 两条路径统一换用，meeting 的档案 JSON 也换成可读渲染；
  (3) build_task_prompt 把 industry_block 移到全部稳定段之后、length_hint 之前。生产数据实测：
  任务路径能力包 5721→1430(-75%)/5348→1161(-78%)/2627→253(-90%)/1070→155(-85%)，
  会议路径 6919→3787(-45%)/6308→3162(-49%)；V4 任务 system 约 16.5k→12.2k(-26%)。批准语义
  不变（技能标题/能力/工作流/指纹可核对，明细在同一 system 的全文块里），sensitive 泄露检测
  继续覆盖注入文本。锁定测试：execution_bundle 去重断言 + industry_knowledge 顺序断言 |
  老板要求"结合最新技术做结构优化,提升产出效果与费用成本" | app/employees.py、
  app/taskrunner.py、app/meeting.py、app/departments.py、tests/test_schema55_execution_bundle.py、
  tests/test_industry_knowledge.py。
- 2026-08-17 | D-046：老板要求把餐饮部（60 岗）与内容部（11 岗）升级到 360 V4 员工的展示
  标准。做法与 V4 同构：为 71 个老岗位接地生成「出厂能力档案」（scope/知识域/数据对象/
  技能树/核心能力/负责决策/工作节奏，素材=各岗真实 duty/steps/inputs/deliverables+已掌握
  技能卡标题）+ 每个技能树节点/能力 60~120 字详细介绍（共 645 条、零缺失），落盘发布内制品
  `app/factory_profiles_v1.json`；`departments.factory_profile_for` 加载，
  `_employee_public_contract` 仅在角色没有真实 V4 档案时以展示层 sidecar 附加
  professional_profile + capability_details。**硬边界**：冻结身份、配置包、任务提示词
  （capabilities_for 的 desc/steps 路径）与 skills_json 技能卡全部零改动；不生成
  tool_permissions/escalation_matrix 等治理字段（避免虚构系统权限）。RID
  `20260817T023516Z-schema55-r10` 已上线并验证（餐饮 109、内容 0/9 档案+详情齐、技能卡
  原样、V4 1601 不受影响、11/11 工位带档案）| 老板："内容和餐饮不如 360，升级到他们的
  标准" | app/departments.py、app/main.py、tests/test_department_capabilities.py、
  服务器生成脚本 /tmp/fpgen.py（71 岗、无联网）。
- 2026-08-17 | D-045：老板反馈 r8 后“能力/技能树还是没有详细介绍”。根因两个：
  (1) `index.html` 的 `?v=54` 手工版本号在 schema55 连续 5 个发版中从未更新，浏览器一直复用旧
  app.js 缓存——改为 `/` 路由按 app.js 内容哈希注入版本号（`_entry_asset_version`/
  `_inject_entry_asset_version`，入口本身 no-store），发版换文件即换 URL，永久杜绝；
  (2)「能力」tab 的 `capabilities_for` 对 V4 员工 name 与 desc 是同一裸标题——能力项新增
  UI 专用 `detail` 字段（来自 capability_details_v1 sidecar），**desc 保持原文不动**（它同时
  是任务提示词载荷，受 test_capability_desc_keeps_the_full_step_text 锁定，不得替换）；
  前端能力卡优先展示 detail、name==desc 的重复裸标题不再渲染。另将档案组重排：技能树/核心
  能力排最前默认展开。RID `20260817T020153Z-schema55-r9` 已上线并验证（能力 4/4 带 detail、
  首页脚本 URL=内容哈希）| 老板连续反馈看不到详情 | app/main.py、app/departments.py、
  static/app.js、tests/test_frontend_navigation.py、tests/test_html_entry_cache_policy.py、
  tests/test_department_capabilities.py、tests/test_v51_frontend_contract.py。
- 2026-08-16 | D-044：技能库 tab 按老板指令移除证据研究面板（evidence_insufficient 状态条/
  发起证据研究/修理厂·全员进修/真实来源/待审核变化），该 tab 收敛为「已掌握技能卡 + 岗位出厂
  能力档案」，空态兜底“该岗位暂无技能档案”；`employeeLearningPanel` 函数保留（管理员侧仍用）。
  同时技能树与核心能力每一项改为可点开条目，点开展示 60~120 字详细介绍：介绍内容按每个 V4 岗位
  自身档案（scope/知识域/数据对象/决策/节奏）接地生成，落盘为发布内制品
  `app/capability_details_v1.json`（做法对齐证据词表 sidecar），由 `_employee_public_contract`
  在 include_profile 时以 `capability_details` 字段随档案下发（登录可见、不走公开 static），
  不入库、不冒充网学技能；文件缺失时优雅降级为纯文本条目 | 老板反馈“这块需要去掉；技能树和
  每个员工能力没有详细介绍和无法点开” | static/app.js（specSkillsTab/employeeProfileRows/
  employeeProfessionalProfile）、app/departments.py（capability_details_for）、app/main.py、
  tests/test_v51_frontend_contract.py、服务器生成脚本 /tmp/capdetails.py（360 岗、无联网）。
- 2026-08-09 | D-001：无头 Claude CLI 采用 `--tools WebSearch`、
  `--allowedTools WebSearch` 与 `--permission-mode dontAsk` 的最小授权组合 |
  真实探针证明现有命令的工具结果被权限层拒绝；官方权限模型支持仅预授权列出的
  工具 | `app/llm.py`、发布预检与工具链测试。
- 2026-08-09 | D-002：`web=True` 必须观察到至少一个成功 WebSearch 工具结果，
  只有调用尝试或模型口头声明不算联网成功 | 防止数字员工、工具箱把无证据回答
  当成有效产出 | 全部联网员工、员工进修、热点/竞品/线索回退。
- 2026-08-09 | D-003：今日必发空选题、格式缺失或权限/受限声明一律失败收口，
  线索雷达继续执行真实原帖 URL 核验 | 宁可失败退款和免费重试，也不交付伪结果 |
  `app/growth.py` 与相关任务终态。
- 2026-08-09 | D-004：工具箱调用必须加载对应员工的职责、启用能力、内部工作方式
  和进修技能；这些内容只进入 private system，公开检索只接收净化后的业务 brief |
  修复“只切模型、不调技能”的结构性缺口 | 六条文本工具与看图写文案。
- 2026-08-09 | D-005：私有 system 不进入进程 argv，改为单次 0700 运行目录中的
  0600 临时文件；CLI 非零退出、I/O 异常、超时和取消均清理独立进程组并失败收口 |
  避免内部岗位资料被本机进程列表读取或失败后继续计费 | `app/llm.py`。
- 2026-08-09 | D-006：图生图供应商没有 private system 边界，采用
  “多媒体师私有视觉规划 → 泄露检查 → 公开编辑指令 → 图片供应商”
  两阶段链路 | 真正运用多媒体师能力，又不把工作方式/技能透传给不支持
  system 边界的生图接口 | 产品图美化。
- 2026-08-09 | D-007：视频混剪中的写稿、画面理解和逐句配画面分别由
  撰稿人/多媒体师的私有工作方式、启用能力和技能库驱动 | 工具箱不得只按
  员工编号切模型 | `app/textvideo.py`。
- 2026-08-09 | D-008：AI 会议的提案、反向验证和老板追问统一调用
  每位参会员工当前的工作方式、启用能力和技能库 | 内容员工与产业专家
  同一契约，能力开关必须生效；主持人排序/决策不冒充具体员工 | `app/meeting.py`。
- 2026-08-09 | D-009：巡店采用“照片证据 → 结构化发现 → 整改项 → 复查事件”
  的领域模型，不把报告 Markdown 当唯一事实源 | 支持责任、期限、门店/区域汇总与审计 |
  巡店经理及老板看板。
- 2026-08-09 | D-010：员工持续沟通复用 task 记录，每轮创建不可变新版本并保持
  单链单叶，不在同一行覆盖结果 | 兼容现有执行、计费、导出和失败退款，阻断分叉 |
  全部内容员工与行业员工直接派活。
- 2026-08-09 | D-011：老板看板采用服务端 tenant + industry 双重范围锁；owner
  不信任任何跨范围查询参数，只有命名 Boss 可切换 | 行业 SaaS 的最小数据隔离边界 |
  dashboard APIs 与下钻。
- 2026-08-09 | D-012：经营看板只展示平台可证明指标；未接 POS/ERP 的营收、毛利、
  库存、人工等只显示行业关注项和待接状态 | 杜绝 AI 编造经营数据 | 首版行业看板。
- 2026-08-10 | D-013：批量门店导入采用“上传解析 → 只读预览 → 原子提交”两阶段，
  以 store_code 而非店名作为更新键 | 防止同名误覆盖、坏行半提交和超时二次导入 |
  schema52 门店主数据。
- 2026-08-10 | D-014：巡店标准分为 mandatory/recommended/operations，地方规则带
  jurisdiction；企业不能无痕关闭适用强制项 | 防止把经营建议冒充法规或随意删法规 |
  行业标准库。
- 2026-08-10 | D-015：每次巡店冻结 effective template snapshot，照片绑定
  capture_slot/item_code，非照片数据绑定 observation | 让历史证据、评分和复查可复盘 |
  巡店创建与详情。
- 2026-08-10 | D-016：销售/员工/库存等只走结构化经营数据通道，视觉模型不接收；
  所有比较维度都带来源/周期/availability/reason_code | 防提示注入、隐私泄漏和假数 |
  门店经营数据与老板看板。
- 2026-08-10 | D-017：首批标准库只承诺当前 11 行业；新增行业通过版本化目录和来源
  审核扩展 | 保证来源可追踪和时效 | 行业目录治理。
- 2026-08-10 | D-018：schema52 导入暂存采用分层留存：未提交预览超过 24 小时后，
  由启动时与周期性、有界、跨租户 sweep 清除加密工作明细；已提交导入把完整
  action 与脱敏数据写入可校验、内容寻址的 zlib 审计归档，再在同一事务删除重复
  working rows | 防止惰性清理遗漏长期不活跃租户，也阻断重复大批量导入造成数据库
  无上限增长；永久保留摘要、来源、目录版本、摘要哈希、操作者与时间，绝不删除
  `store_branch`、`inspection_business_value`、`inspection_visit` 等权威业务记录 |
  schema52 导入留存与审计。
- 2026-08-10 | D-019：用户已明确要求“部署到线上去”，schema52 获得生产发布授权；
  授权不等于发布已完成，仍须通过最终独立 P0/P1 复审、最终回归、确定性不可变构建、
  固定升级器切换及正式域名验收后才可声明上线 | 防止把一句部署指令错误记录成
  已部署事实 | schema52 生产发布门禁。
- 2026-08-10 | D-020：所有 SQLite 连接启用 `secure_delete=ON`，留存 sweep
  提交后执行 WAL truncate checkpoint；长读者导致 busy 时不回滚逻辑清理，
  由启动与 30 分钟周期扫描重试 | 逻辑删除不代表 WAL 取证层已无
  可解密密文；必须同时证明主库、官方备份与空闲 WAL 均不再含旧 token |
  schema52 预览个人信息留存。
- 2026-08-11 | D-021：餐饮 60 名员工保持不动；其余 10 个行业从每行业 36 名模板岗位
  重建为每行业 6 名痛点驱动的决策员工 | 本地审计证明 10 行业有 35 个共同岗位名，
  继续保留人数只会制造虚假专业度 | 行业目录、办公室、专家匹配与营销口径。
- 2026-08-11 | D-022：V2 员工目录使用独立 `data/industry_decisions`，每个员工将
  trigger、required_inputs、evidence、decision_states、approval_boundary 和
  forbidden_actions 作为结构化合同 | 行业差异必须可由代码和测试验证，不能只存在于
  提示词散文 | 配置加载、公开任务指南、执行提示词与发布预检。
- 2026-08-11 | D-023：V2 使用 20000～29999 全新编号；旧 360 个模板员工完整保留为
  legacy，只服务既有任务、线程和历史会议，不能新派活或进修 | 复用旧 idx 会把历史任务、
  employee_config、计费归因和会议行动静默解释为新岗位 | 员工 resolver、任务中心、会议、
  看板与专家匹配。
- 2026-08-11 | D-024：行业员工 V2 升级到 schema53，为 task 冻结员工 key、目录版本、
  名称、部门和规格摘要，并为 thread/meeting 保存版本身份或成员快照 | 只靠永久保留 JSON
  仍无法抵御未来目录变化；历史解释必须由任务自身证据锁定 | 数据迁移、幂等、重试、
  跟进、报表和发布回滚。
- 2026-08-11 | D-025：V2 员工只输出建议、证据包和审批卡；采购、改价、退款、排班、
  停业、召回、医疗诊断/处方和安全放行等高风险动作一律需要有权人员确认 | AI 的行业能力
  不能扩大系统现有写权限或替代法定责任人 | 全部行业员工运行时与产品文案。
- 2026-08-12 | D-026：撤销 D-021 与 D-023 的“每行业 6 名现役、旧 360 名员工
  legacy/降级”产品口径；原 10 个非餐饮行业 360 名员工全部保持在岗，并按原工号
  `1001–1936` 重建为 360 个行业痛点专属岗位 | 用户明确要求不减员、不降级；联网研究
  证明各行业存在足够多高频、高价值、可取证的独立决策岗位 | 行业目录、办公室、派活、
  营销口径与验收计数。D-021/D-023 仅保留为被本决策取代的历史记录。
- 2026-08-12 | D-027：员工与岗位版本分离；`idx` 是稳定员工/工位号，当前岗位身份使用
  完整 `identity_ref`，旧 schema52 岗位和 20xxx V2 只作为旧任务的历史岗位版本，
  不计为离职或降级员工 | 同一人在岗但职责升级时，既要保住历史任务可追溯性，又不能
  把旧提示词、技能、模型和权限静默贴到新岗位 | schema54、员工配置、任务线程、会议、
  看板与前端状态文案。
- 2026-08-12 | D-028：现役口径固定为 431：核心 11 + 餐饮 60 + 非餐饮 360；行业员工
  现役 420。当前非餐饮岗位采用原工号和 `2026.08.v3` 版本，十行业各 36 名、八组
  `5/5/5/5/5/4/4/3` 完整占位 | 数量、工号和岗位价值必须同时可验收，不能用 6 个伞形岗
  或模板填充冒充行业深度 | 目录加载、发布预检、公开员工卡、测试与文档。
- 2026-08-12 | D-029：360 个 V3 岗位必须通过“唯一主决策 + 独有触发器 + 4～8 项
  必需输入 + 可复核证据 + 专属输出 + 可计算指标”门禁；岗位名、key、主决策、核心输入
  与指标组合不得在同一行业重复 | 联网研究的痛点强度、使用频率、经济价值和数据可得性
  四维评分用于排序，低频填充岗不得挤占高价值岗位 | 行业 JSON、运行时提示、QA 与独审。
- 2026-08-12 | D-030：旧任务可按原岗位版本查看、免费重试和在线程内继续沟通；明确的
  “交给当前岗位重新做”必须创建新根任务并使用当前 V3 岗位。任何历史配置档案缺失均
  fail-closed，不能回退到当前岗位，也不能先扣费 | 保护连续沟通，同时杜绝同工号串岗 |
  task、thread、meeting、billing 与任务详情。
- 2026-08-12 | D-031：本轮只获准修改 `/Users/wanglei/Documents/派活AI-R7`，明确
  禁止部署、禁止复用旧候选 RID、禁止把本地测试通过描述为已上线 | 用户授权边界 |
  schema54 的全部实现、QA、评审与交付。
- 2026-08-12 | D-032：360 名 V3 员工不得仅替换提示词、名称或技能标签；
  每人必须冻结独立的 `professional_profile`，含岗位范围、负责决策、行业知识域、
  真实数据对象、只读工具权限、技能树、专业能力、工作节奏、升级矩阵和进修路径；
  工作流、交付物、数据对象、技能树与能力在去掉岗位名和通用安全语句后仍须通过
  反复制相似度门禁 | 用户明确指出“光换提示词没有用”，岗位档案必须真正驱动
  执行、会议、进修和交付 | V3 目录、schema54 role config、任务提示、员工面板与 QA。
- 2026-08-12 | D-033：V3 的 36 岗不继续共用 V2 的 6 个伞形痛点；每行业必须按
  8 个真实经营/合规痛点簇重建，并以痛点强度、使用频率、经济价值、数据可得性
  四维评分确定 1–36 优先级，不得按工号或生成顺序伪造排名 | 保证 360 人是
  高频、高价值的行业解决岗，而不是用泛化岗位补数 | 痛点目录、优先级、公开岗位卡与验收测试。
- 2026-08-12 | D-034：V3 “专业度”采用三层 fail-closed 门禁：目录加载、自动化测试与发布预检
  同时拒绝通用占位工具、统一首尾步骤、固定三段式学习路径、空泛升级条件和治理指标套壳；
  每人至少 4 个真实专业执行步骤、3 项指标中至少 2 项岗位原生业务 KPI，升级必须绑定该岗位真实信号、
  责任人与动作 | 抽查发现首版茶咖档案虽有真实工具和技能，合同骨架仍是字段替换；表面不同不等于独立岗位 |
  V3 生成器、`app/departments.py`、`deploy/preflight.py`与行业目录 QA。
- 2026-08-13 | D-035：用户最新明确要求“现在把完成的更新项目同步到线上”，授权
  schema54 通过固定升级链发布到生产；本授权仅取代 D-031 的“不部署”边界，不取代
  D-026～D-034 的 360 人全部在岗、岗位独立专业化、历史身份隔离和 fail-closed 合同。
  只有唯一升级回执 `succeeded/complete`、schema54、431 人口径、切换后备份与正式
  HTTPS 全部通过后才能声明上线 | 区分发布授权与发布完成，防止授权覆盖产品质量门禁 |
  RID `20260813T134506Z-schema54-r7` 的受控生产发布。
- 2026-08-13 | D-036：schema54/V3 不满足“真实专属员工”验收：V3 的岗位标题虽已
  专属化，但 `person` 360/360 沿用 V1 模板名并被前端主显示；生产 V3 360 人中
  `learned_at` 和联网技能均为 0。禁止用 `display_name`、换提示词或静态技能树掩盖问题，
  改为不可变 V4 新身份 + schema55 版本化 role bundle | 用户在线验收明确指出姓名未改、
  未完成全网进修且能力不足；原地改 V3 会破坏 identity/spec hash 和历史任务解析 |
  360 人姓名、目录版本、任务/线程/会议与配置迁移。
- 2026-08-13 | D-037：全网进修的完成口径固定为“真实查询与抓取证据 + 来源/claim 回链 +
  知识/技能/能力/工作流提案 + 人工审批 + 新 bundle/config revision 激活”；模型自由文本
  `source`、静态 `learning_tracks` 和任务时临时联网均不算进修完成 | 现有实现只写
  `skills_json`，不捕获 WebSearch 来源，也不更新岗位档案/能力/工作流 | schema55
  learning batch/run/source/artifact、审批、任务与会议注入。
- 2026-08-13 | D-038：本轮用户反馈授权修复源码和本地验证，但不自动继承 D-035 的
  schema54 单次生产发布授权，也不自动授权 360 人付费批量进修；Schema55 新 release 与
  批量预算须分别取得明确授权 | 防止一次历史发布授权被无限复用，或在未展示成本上限时
  扣除最多 360 人的学习积分 | 发布门禁、批量学习预算与生产执行。
- 2026-08-13 | D-039：用户已明确要求“全部跑完并且启动真实联网进修并且部署”，授权
  通过固定升级链构建并发布全新的 schema55/V4 release，并在部署验证通过后为 360 个
  V4 current 岗位启动真实联网研究。批次内部预算上限固定为 `360 × 3 = 1080` 点；总部
  `tenant=1` 的正式合同为 `platform_included`、钱包实扣 `0`，但真实搜索与模型调用仍须
  受并发、来源、失败熔断和供应商配额门禁约束。该授权允许执行研究，不允许绕过逐项
  证据校验、人工审批、四元 CAS 或把失败/待审条目冒充已进修完成；只有新 bundle/config
  revision 激活后才计为完成 | 取代 D-038 的“未授权”状态，但保留 D-036/D-037 的身份
  版本和证据型进修完成口径 | schema55 正式发布、360 人研究批次、审批与线上验收。
- 2026-08-16 | D-041：老板报“餐饮/内容技能库不见了”，缺陷猎杀确认为**前端回归、非数据丢失**：
  行业专家面板 `specSkillsTab` 被改成只渲染证据型 `employeeLearningPanel`，不再渲染 V1
  legacy `skills`（餐饮 59 人×6 条、内容部另走可正常显示的 `skillsTab`）与 V4
  `professional_profile.skill_tree`（360 人各 5 条）。生产库与全部 schema55 备份中这些技能
  数量恒定（69 条 legacy + 360 份技能树），未被本会话或任何发布改动。修复只在
  `static/app.js` 的 `specSkillsTab` 恢复“已掌握技能 + 岗位技能树”展示并保留证据进修面板，
  零数据变更；内容部路径本就正常不改动 | 老板明令“技能库不许乱动”，须先证明数据完好再恢复
  可见，且不得为了“看起来有”而改动或伪造技能数据 | `static/app.js` `specSkillsTab`、
  `tests/test_v51_frontend_contract.py` 新增回归测试、下一枚 schema55 release 的前端切片。
- 2026-08-16 | D-043：老板否决 gate v2 放宽，明确“按餐饮数字员工和内容部门员工的标准来”。
  餐饮/内容技能来自老的 `employees.learn`（联网研究→技能卡 title+detail+source，无严格证据门禁），
  V4 被 HTTP 端点 `employee_learn` 一行 409 挡在外、强制走过不去的证据门禁。实测把老标准直接用于
  360：机制可用（1101 学到 6 卡 $0.24）但**内容跑偏**——生成通用自媒体/营销技能，与"日内客流需求官"
  等分析岗无关（岗位合成名搜不到→模型漂移）。故采用“接地版老标准”：同一 learn 标准，但用每个岗位
  真实 V4 档案（scope/knowledge_domains/data_objects/skill_tree）锚定联网研究方向、并禁止无关营销
  内容；产出紧扣本岗位专业领域的技能卡，`employees.set_skills` 干净写入 skills_json；part1a 的技能库
  tab 直接显示（无需发版）。1101 的跑偏技能由接地版替换。批处理 3 并发、约 $0.25/岗、~2h、
  可续跑（/tmp/grounded_done.txt，mode=ro 读 WAL）。**不改证据门禁代码**（strict 保留，只是 360 展示
  技能不再走这条路）；餐饮/内容技能不动 | 老板明确“不要 gate v2，按餐饮/内容标准来” | 现有
  `employees.learn`/`set_skills` + 接地 prompt（服务器脚本）；360 skills_json 与技能库展示。
- 2026-08-16 | D-042：老板要求给 360 行业员工上“真技能”。核实：360 有真实但“标题化”的 V4
  岗位档案（skill_tree/capabilities 是短标题，另有 scope/decisions/escalation/operating_rhythm/
  tool_permissions/data_objects/knowledge_domains 等真实内容），但没有像餐饮那样“标题+怎么做”的
  learned 详细技能卡（skills_json 为空、已激活进修 0）。选“组合拳”：(1) part1a 前端把 360 的
  「技能库」tab 渲染完整出厂能力档案（employeeProfessionalProfile），明确标注“岗位出厂能力·非
  全网进修”，让全部 360 立刻有实质内容、零数据变更；(2) part2 修证据门禁三道墙 + curated 权威源，
  用真进修给“能过”的岗位叠加带真实来源的“网学技能”。出厂内容不谎称网学、失败/零证据不计完成，
  守住 D-037/D-040 红线；餐饮/内容技能不再改动 | 老板明确“360 只有名字没技能、这最重要”，且坚持
  不造假 | static/app.js specSkillsTab、tests/test_v51_frontend_contract.py、RID
  20260816T121451Z-schema55-r7（part1a 已上线）；part2 证据门禁重建待续。
- 2026-08-16 | D-040：用户明确要求“不用审批直接进修”。证据门禁、四元 CAS、失败熔断
  与“只有新 bundle/config revision 激活才算完成”仍有效；研究一旦通过门禁，worker
  立即走与审批相同的 CAS 激活路径，不再把通过证据的提案留在修理厂待审。失败或零直接
  证据仍不得计为完成。客户端仍禁止提交 `auto_approve=true` | 取代 D-039 的人工逐项
  审批要求 | 学习研究 worker 自动激活与线上 360 人批次。
