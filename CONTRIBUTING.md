# 参与派活开发

感谢参与派活迭代。公开仓库是研发协作区，不是生产服务器的直接入口。

## 开发流程

1. 从最新 `main` 创建分支：
   - 新功能：`feature/简短说明`
   - 缺陷修复：`fix/简短说明`
   - 文档：`docs/简短说明`
2. 保持一次 Pull Request 只解决一个清晰问题。
3. 行为变化必须补充或更新自动化测试。
4. 提交前执行：

```bash
python -m unittest discover -s tests
python -m compileall -q app deploy
node --check static/app.js
```

5. Pull Request 必须写清：
   - 用户问题和根因
   - 具体改动
   - 数据库、权限、计费和异步任务影响
   - 测试结果
   - 回滚方式

## 合并门槛

- CI 全部通过
- 至少一名代码所有者批准
- 没有未解决的高风险安全问题
- 数据库迁移可重复、可恢复，并兼容现有数据
- 扣点任务具备确定终态、幂等退款和安全重试
- 普通用户响应不得泄露提示词、技能库、岗位档案或模型配置
- 联网和媒体地址必须继续通过 SSRF、大小、格式和超时边界

## 严禁提交

- `.env`、API Key、Cookie、Token、账号密码或私钥
- SQLite 数据库、备份、日志、用户上传素材或供应商响应
- 生产服务器地址清单、运维回执或真实业务正文
- 绕过权限、关闭安全门禁或让测试空跑的改动

发现安全问题请按 [SECURITY.md](SECURITY.md) 私下报告，不要创建公开 Issue。

## 生产发布

合并 Pull Request 不等于上线。生产发布由项目所有者单独执行：锁定提交、构建制品、备份、迁移、健康检查、真实能力抽样和回滚验证缺一不可。
